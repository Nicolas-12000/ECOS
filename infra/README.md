# Infra

Docker and local infra files.

- docker-compose.spark.yml: Spark master/worker for local PySpark pipelines.
- docker-compose.spark.recovery.yml: Recovery stack with health checks and restart policy when Spark fails to boot.
